# Google Cloud project (for BigQuery access)
# Create a project at console.cloud.google.com, enable BigQuery API
# Then: gcloud auth application-default login
GCP_PROJECT_ID=your-gcp-project-id

# Optional: path to service account key JSON (alternative to gcloud auth)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/key.json
